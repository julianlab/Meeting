{% if control == 'subscribed' %}
    <a href="{{ path('event', {'id':evento.id}) }}" class="btn btn-secondary">Mas info</a>
    <a href="{{ path('unjoinEvent', {'id':evento.id}) }}" class="btn btn-secondary">Unjoin</a>
{% endif %}
{% if control == 'created' %}
    <a href="{{ path('event', {'id':evento.id}) }}" class="btn btn-secondary">Mas info</a>
    {% if evento.isActive == 1 %}
        <a href="{{ path('disableEvent', {'id':evento.id}) }}" class="btn btn-secondary">Disable</a>
    {% endif %}
    {% if evento.isActive == 0 %}
        <a href="{{ path('enableEvent', {'id':evento.id}) }}" class="btn btn-secondary">Enable</a>
    {% endif %}{% endif %}
{% if control == 'social'%}
    <a href="{{ path('event', {'id':evento.id}) }}" class="btn btn-secondary">Mas info</a>
    <a href="{{ path('joinEvent', {'id':evento.id}) }}" class="btn btn-secondary">Join</a>
{% endif %}
{% if control == 'main' %}
    <div class="row align-items-center justify-content-center">
    {% if evento.isActive == 1 and creator == 1 %}
        <a href="{{ path('disableEvent', {'id':evento.id}) }}" class="btn btn-secondary">Disable</a>
    {% endif %}
    {% if evento.isActive == 0 and creator == 1%}
        <a href="{{ path('enableEvent', {'id':evento.id}) }}" class="btn btn-secondary">Enable</a>
    {% endif %}
    {% if joined == 1 %}
        <a href="{{ path('unjoinEvent', {'id':evento.id}) }}" class="btn btn-secondary">Unjoin</a>
    {% endif %}
    {% if joined == 0 %}
        <a href="{{ path('joinEvent', {'id':evento.id}) }}" class="btn btn-secondary">Join</a>
    {% endif %}
    </div>
{% endif %}